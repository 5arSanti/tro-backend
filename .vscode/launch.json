{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Docker FastAPI",
      "type": "debugpy",
      "request": "attach",
      "host": "localhost",
      "port": 5678,
      "justMyCode": true,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}

